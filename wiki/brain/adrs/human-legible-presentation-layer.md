---
title: "The briefing is a build-time derivation; summaries live in frontmatter; attention verdicts live on inbox items"
kind: decision
status: accepted
updated: 2026-07-12
confidence: medium
summary: >
  Three bets: executive summaries are a frontmatter field flowing
  through the views pipeline; the briefing is a dependency-free
  build-time Astro page over pages.json + inbox + state.md and
  becomes the app's landing surface; attention judgement is two
  small verbs on the existing inbox (judge, grade), recorded only
  in-session.
sources:
  - ../prds/human-legible-presentation-layer.md
  - ../../../sources/conversations/2026-07-12--ui-rethink-and-rewrite-question.md
---

# The briefing is a build-time derivation; summaries live in frontmatter; attention verdicts live on inbox items

**Decision.** Three bets, one per moving part. **Summaries:**
`summary:` is an optional frontmatter field (one to three
sentences), validated for shape when present, exported through
`pages.json`; the validator warns — never fails — on live
commitment-class pages that lack one, and `/groom` owns drift
checking. **The UI:** a complete rewrite of the
layer, not an extension — Starlight (the generic docs theme)
retires; `ui/` becomes a purpose-built Astro app whose root *is*
the briefing, rendered at build time from data the repo already
derives — `pages.json` for kinds/statuses/appetites/summaries, the
inbox directory for pending work and verdicts, `state.md` for the
orientation strip. Wiki pages render inside the same app at their
existing paths with lifecycle-aware chrome (kind, status,
confidence, summary, the ai-suggestion banner); Pagefind stays for
search; the dependency set shrinks to Astro + Pagefind. No
client-side data fetching, no graph/chart dependencies — anything
visual is computed at build time into plain markup. **Attention:** two verbs on the existing
inbox — `inbox judge <id>` stamps a verdict
(needs-operator / fyi / routine, routine being the default under
uncertainty) plus a one-line reason onto the item's JSON;
`inbox grade <id>` records the operator's useful/noise assessment
into `wiki/_state/attention-grades.json`, which the tend skill
reads back as calibration before judging. Verdicts are only ever
written in-session; producers and the schedule stay LLM-free.

## Context

The PRD commits to an opinionated, Shape Up-native surface without
naming where each piece lives. The forces: the UI must stay
local-first and serving-safe (a static build behind the existing
serve fallback); the kernel must stay dependency-light after a week
of deleting surfaces (a charting library is a supply-chain surface
the security persona flagged on the pitch); judgement must be
auditable and cheap to correct (the alarm-fatigue rabbit hole); and
nothing may fork the content — humans and agents read derivations
of the same wiki. Every data input the briefing needs already
exists in the repo; the decision is to *derive* rather than
introduce state.

## Alternatives

- **Purpose-built Astro app + frontmatter summaries + inbox
  verbs** *(chosen)* — the layer is rewritten around the briefing
  (operator direction, mid-shaping: "a complete rewrite of the UI
  layer, not an extension"); dependencies shrink (Starlight out),
  zero new stores, rebuilt by the self-heal loop that already
  exists; judgement rides the inbox the tend session already
  touches.
- **Extend Starlight with a briefing page** — the pre-correction
  shape of this ADR: cheapest, but the frame stays a generic docs
  site and the opinion lives in an annex; the operator rejected it
  explicitly. Superseded in-flight by the rewrite bet.
- **A live server-rendered briefing** (the `/dash` pattern grown
  up) — always-fresh, but duplicates the rendered site's styling
  layer in Python strings and dies in serving mode; the static
  build already self-heals within a minute of wiki changes, which
  is fresh enough for a briefing. Rejected.
- **Client-side briefing** (fetch pages.json + inbox from the
  browser) — needs the JSON API in every deployment, breaks on
  static-only serving, adds script surface for Sam to review.
  Rejected.
- **Summaries in a sidecar file or generated by the UI** — either
  forks content away from the page it summarises or puts an LLM in
  the build path. Frontmatter keeps the summary versioned with its
  body and maintained by the same agent edit. Rejected.
- **A dedicated attention store/service** — over-mechanism; the
  inbox item is already the unit of pending attention, and grades
  are a small JSON the tend skill reads. Rejected.

## Consequences

- **Opens** the partner surface: every band on the briefing is
  reproducible from repo state, so "why is this here?" always has
  an answer.
- **Opens** cheap extension: a new card kind is one Astro component
  over fields `pages.json` already carries.
- **Costs** freshness: the briefing lags the wiki by one rebuild
  (~a minute via the self-heal kick). Accepted for a surface read a
  few times a day.
- **Costs** discipline: summaries are only as good as the agent's
  edit habit; the validator warning and `/groom`'s drift check are
  reminders, not gates. Accepted, revisitable if drift appears.
- **Costs** the rewrite itself: navigation, page chrome, search
  wiring, and the onboarding deck are re-implemented rather than
  inherited from the theme. Accepted — the theme's genericity was
  the problem being solved.
- **Retains** every standing guarantee: read-only UI, no second
  content store, no scheduled LLM, serving mode unchanged; wiki
  page URLs keep their existing shape so links and bookmarks
  survive.
