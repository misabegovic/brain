---
title: <the question, phrased as a question — e.g. "Should views support LLM-digest blocks?">
kind: topic
status: draft            # draft=open · living=active · superseded=graduated · archived=dropped
updated: YYYY-MM-DD
confidence: low
sources:
  - <where the question came from — a capture, a source, a page>
---

# <the question>

## Question

One paragraph: what is being discussed, why it matters now, and
what a resolution would unblock. Name the scope (repo / org /
brain) implicitly by where this file lives.

## Discussion

Dated entries, newest first. Each entry is one voice at one moment —
operator or named agent (`by:`), a position, and its reasoning.
Entries are append-only; changing your mind is a new entry.

- **YYYY-MM-DD** (`by: <who>`) — <position / argument / new
  information>.

## Outcome

`(open)` while undecided. On resolution: one sentence naming what
was decided, dated, and — when the decision is commitment-class —
the link to the ADR/PRD it graduated into via `/shape` (set this
page `status: superseded`, `superseded_by:` that artifact). Small
decisions that don't warrant an ADR are recorded here directly;
this page then stays `living` as the record.
